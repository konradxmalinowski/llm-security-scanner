---
phase: 05-advanced-features
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/llm_scanner/baselines/__init__.py
  - tests/test_baseline.py
autonomous: true
requirements:
  - ADV-02
must_haves:
  truths:
    - "BaselineManager(output_dir).save(name) copies the most recent report.json in output_dir subdirectories to output_dir/baselines/{name}.json"
    - "BaselineManager(output_dir).load(name) returns a ScanReport deserialized from output_dir/baselines/{name}.json"
    - "diff_findings(baseline, current) returns only findings that are success=True in current AND not success=True in baseline (new vulnerabilities)"
    - "Baseline name validation rejects names with path traversal characters (../../ etc) — only alphanumeric, hyphen, underscore allowed"
    - "save() raises FileNotFoundError when no report.json exists in output_dir"
  artifacts:
    - path: "src/llm_scanner/baselines/__init__.py"
      provides: "BaselineManager class with save(), load(), diff_findings()"
      exports: ["BaselineManager"]
    - path: "tests/test_baseline.py"
      provides: "Baseline save/load/diff unit tests"
  key_links:
    - from: "src/llm_scanner/baselines/__init__.py"
      to: "src/llm_scanner/models.py"
      via: "imports ScanReport, AttackResult"
      pattern: "from llm_scanner.models import AttackResult, ScanReport"
---

<objective>
Create the baselines module that implements named baseline save/load and finding diff logic. This is the data-layer half of ADV-02; the CLI subcommands (baseline save / baseline compare) are wired in Plan 05-05.

Purpose: Baseline comparison lets teams detect regressions — new vulnerabilities introduced since the last approved scan. The module is intentionally thin: save copies a report file, load deserializes it, diff is a set operation on attack_ids.

Output: baselines/__init__.py, passing baseline tests.
</objective>

<execution_context>
@/Users/konrad.malinowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konrad.malinowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-advanced-features/05-RESEARCH.md
@.planning/phases/05-advanced-features/05-PATTERNS.md
@.planning/phases/05-advanced-features/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create baselines module</name>
  <files>src/llm_scanner/baselines/__init__.py</files>
  <read_first>
    - src/llm_scanner/reporters/json_reporter.py — write pattern: path.write_text(report.model_dump_json(indent=2), encoding="utf-8")
    - src/llm_scanner/models.py — ScanReport.model_validate_json() for deserialization; AttackResult fields (attack_id, success)
    - .planning/phases/05-advanced-features/05-PATTERNS.md — baselines/__init__.py pattern section: read pattern (model_validate_json), write pattern (model_dump_json), diff_findings set logic, path safety rule (re.match alphanumeric + -_)
    - .planning/phases/05-advanced-features/05-RESEARCH.md — Pattern 5: diff logic definition, BaselineManager class structure
  </read_first>
  <behavior>
    - BaselineManager(output_dir).__init__ stores output_dir as self._output_dir
    - _validate_name(name) raises ValueError with descriptive message if name does not match r'^[a-zA-Z0-9_-]+$'
    - save(name) calls _validate_name(name); finds all report.json files via self._output_dir.rglob("report.json") excluding the baselines/ subdirectory; sorts by file mtime and picks the most recent; raises FileNotFoundError("No report.json found in output_dir") if none found; creates self._output_dir / "baselines" directory; writes the report.json content to baselines/{name}.json; returns the path
    - load(name) calls _validate_name(name); constructs path = self._output_dir / "baselines" / f"{name}.json"; raises FileNotFoundError with message f"Baseline '{name}' not found at {path}" if path does not exist; reads content and returns ScanReport.model_validate_json(content)
    - diff_findings(baseline, current) is a @staticmethod; returns [f for f in current.findings if f.success and f.attack_id not in {b.attack_id for b in baseline.findings if b.success}]
  </behavior>
  <action>
    Create src/llm_scanner/baselines/ directory with __init__.py. Start with `from __future__ import annotations`. Imports: pathlib.Path, re, llm_scanner.models (AttackResult, ScanReport).

    Define BaselineManager class with __init__(self, output_dir: Path).

    Define _validate_name(self, name: str) -> None: if not re.match(r'^[a-zA-Z0-9_-]+$', name): raise ValueError(f"Invalid baseline name {name!r}. Use only letters, digits, hyphens, and underscores.").

    Define save(self, name: str) -> Path: validates name; finds all report.json files with self._output_dir.rglob("report.json"), filters out those under self._output_dir / "baselines" (to avoid loading previously saved baselines as the source); sorts by path.stat().st_mtime descending; raises FileNotFoundError if empty; reads most recent via report_json_path.read_text(); creates baselines_dir = self._output_dir / "baselines"; baselines_dir.mkdir(parents=True, exist_ok=True); writes content to baselines_dir / f"{name}.json"; returns dest_path.

    Define load(self, name: str) -> ScanReport: validates name; constructs path; raises FileNotFoundError if not exists; returns ScanReport.model_validate_json(path.read_text()).

    Define diff_findings(baseline: ScanReport, current: ScanReport) -> list[AttackResult] as @staticmethod: baseline_ids = {f.attack_id for f in baseline.findings if f.success}; return [f for f in current.findings if f.success and f.attack_id not in baseline_ids].

    __all__ = ["BaselineManager"]
  </action>
  <verify>
    <automated>cd "/Users/konrad.malinowski/Documents/Konrad/AI-Engineer/AI/LLM Security Scanner" && python -c "from llm_scanner.baselines import BaselineManager; print('OK')" && python -m ruff check src/llm_scanner/baselines/</automated>
  </verify>
  <done>
    - BaselineManager imports cleanly
    - ruff check reports zero errors on baselines/
    - No regression in existing tests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_baseline.py</name>
  <files>tests/test_baseline.py</files>
  <read_first>
    - tests/test_reporters.py — fixture for ScanReport and AttackResult construction (all required fields); tmp_path usage
    - src/llm_scanner/baselines/__init__.py — BaselineManager.save(), load(), diff_findings() signatures
    - src/llm_scanner/models.py — ScanReport constructor, model_dump_json() output format (to write valid test fixture JSON)
  </read_first>
  <behavior>
    - test_baseline_save_creates_file: After writing a valid report.json to tmp_path/"scan1/" subdir, BaselineManager(tmp_path).save("prod") creates tmp_path/"baselines/prod.json" and returns that path
    - test_baseline_save_file_exists: The saved baseline file contains valid JSON parseable as a ScanReport
    - test_baseline_load_roundtrip: save("test") followed by load("test") returns a ScanReport with the same target and risk_score as the original
    - test_baseline_load_missing: load("nonexistent") raises FileNotFoundError
    - test_baseline_save_no_reports: save("x") raises FileNotFoundError when no report.json exists under output_dir
    - test_baseline_name_validation_rejects_traversal: save("../../etc") raises ValueError
    - test_baseline_name_validation_rejects_slash: save("prod/v1") raises ValueError
    - test_baseline_diff_new_finding: diff_findings(baseline, current) returns findings that are success=True in current and not success=True in baseline
    - test_baseline_diff_no_regression: diff_findings returns empty list when all current successful findings were also successful in baseline
    - test_baseline_diff_fixed_finding: finding that was success=True in baseline but success=False in current does NOT appear in diff (it was fixed, not new)
  </behavior>
  <action>
    Create tests/test_baseline.py. Import header: `from __future__ import annotations`, json, pathlib.Path, datetime (UTC, datetime), pytest, llm_scanner.models (AttackResult, ScanReport, Severity), llm_scanner.baselines (BaselineManager).

    Helper: make_scan_report(target: str = "http://test", risk_score: float = 2.5, findings: list | None = None) -> ScanReport to construct minimal valid ScanReport. findings defaults to [].

    Helper: write_report_json(directory: Path, report: ScanReport) -> None that creates directory, writes report.model_dump_json(indent=2) to directory/"report.json".

    For diff tests, construct two ScanReports with overlapping and non-overlapping findings. Use Severity.HIGH for test findings with success=True and Severity.LOW with success=False for baseline findings.

    For test_baseline_save_creates_file: create tmp_path/"scan1/", write report.json, call BaselineManager(tmp_path).save("prod"), assert (tmp_path/"baselines/prod.json").exists().
  </action>
  <verify>
    <automated>cd "/Users/konrad.malinowski/Documents/Konrad/AI-Engineer/AI/LLM Security Scanner" && python -m pytest tests/test_baseline.py -xq</automated>
  </verify>
  <done>
    - All 10 baseline tests pass
    - python -m pytest tests/ -q shows no regressions
  </done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by Plan 05-04:

**src/llm_scanner/baselines/__init__.py (new):**
- `BaselineManager` (class)
- `BaselineManager.__init__(self, output_dir: Path)`
- `BaselineManager._validate_name(self, name: str) -> None` (raises ValueError on invalid name)
- `BaselineManager.save(self, name: str) -> Path` (copies most recent report.json to baselines/{name}.json)
- `BaselineManager.load(self, name: str) -> ScanReport` (deserializes saved baseline)
- `BaselineManager.diff_findings(baseline: ScanReport, current: ScanReport) -> list[AttackResult]` (@staticmethod)

**tests/test_baseline.py (new):**
- test_baseline_save_creates_file, test_baseline_save_file_exists, test_baseline_load_roundtrip, test_baseline_load_missing, test_baseline_save_no_reports, test_baseline_name_validation_rejects_traversal, test_baseline_name_validation_rejects_slash, test_baseline_diff_new_finding, test_baseline_diff_no_regression, test_baseline_diff_fixed_finding

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI --name arg → BaselineManager | User-controlled baseline name string used to construct filesystem path |
| baselines/{name}.json → ScanReport | Baseline file read from filesystem; could be modified outside scanner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | Path traversal via baseline --name | mitigate | _validate_name() enforces r'^[a-zA-Z0-9_-]+$' via re.match; names with "/" ".." or special chars raise ValueError before any filesystem operation |
| T-05-11 | Tampering | Tampered baseline file alters diff results | accept | Baselines are local operator-controlled files; no integrity mechanism needed for local-use CLI tool; operator controls the output directory |
| T-05-12 | Information Disclosure | Baseline JSON contains full scan findings including judge_reasoning | accept | Baseline files are stored in user's chosen output_dir; same sensitivity as report.json already produced by scanner |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install steps in this plan |
</threat_model>

<verification>
- python -m pytest tests/test_baseline.py -xq: all 10 tests pass
- python -m ruff check src/llm_scanner/baselines/: zero errors
- python -c "from llm_scanner.baselines import BaselineManager; print('OK')": exits 0
- python -m pytest tests/ -q: full suite green
</verification>

<success_criteria>
- BaselineManager.save() writes JSON to baselines/{name}.json and rejects path traversal names
- BaselineManager.load() returns a valid ScanReport or raises FileNotFoundError
- diff_findings() correctly identifies net-new vulnerabilities vs baseline
- All 10 tests pass; zero ruff errors
</success_criteria>

<output>
Create .planning/phases/05-advanced-features/05-04-SUMMARY.md when done.
</output>
