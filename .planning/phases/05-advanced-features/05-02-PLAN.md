---
phase: 05-advanced-features
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/llm_scanner/reporters/sarif.py
  - src/llm_scanner/reporters/__init__.py
  - tests/test_sarif.py
autonomous: true
requirements:
  - ADV-04
must_haves:
  truths:
    - "`--format sarif` (wired in Plan 05-05) writes report.sarif to the scan output directory"
    - "report.sarif has top-level keys: $schema == 'https://json.schemastore.org/sarif-2.1.0.json', version == '2.1.0', runs (list with one item)"
    - "SARIF results list contains only findings where success=True AND suppressed=False"
    - "Every SARIF result location has artifactLocation.uri == '.' (non-empty, satisfies GitHub upload-sarif validator)"
    - "SARIF severity levels: CRITICAL/HIGH -> error, MEDIUM -> warning, LOW/INFO -> note"
    - "get_file_reporter('sarif') returns a SarifReporter instance without error"
  artifacts:
    - path: "src/llm_scanner/reporters/sarif.py"
      provides: "SarifReporter class with save() and _build() methods"
      exports: ["SarifReporter"]
    - path: "src/llm_scanner/reporters/__init__.py"
      provides: "Updated registry with sarif key"
      contains: "'sarif': SarifReporter"
    - path: "tests/test_sarif.py"
      provides: "SARIF structure and content tests"
  key_links:
    - from: "src/llm_scanner/reporters/__init__.py"
      to: "src/llm_scanner/reporters/sarif.py"
      via: "import SarifReporter"
      pattern: "from llm_scanner.reporters.sarif import SarifReporter"
    - from: "src/llm_scanner/reporters/sarif.py"
      to: "src/llm_scanner/models.py"
      via: "imports ScanReport, Severity"
      pattern: "from llm_scanner.models import ScanReport, Severity"
---

<objective>
Create SarifReporter and register it in the reporters package so that --format sarif produces a valid SARIF 2.1.0 JSON file consumable by GitHub Security tab and VS Code SARIF Viewer.

Purpose: SARIF is the industry standard for security tool output. This plan slots SarifReporter into the existing _REPORTER_FACTORIES registry — no CLI changes needed until Plan 05-05 wires --format sarif into the parser.

Output: sarif.py, updated reporters/__init__.py, passing SARIF tests.
</objective>

<execution_context>
@/Users/konrad.malinowski/.claude/get-shit-done/workflows/execute-plan.md
@/Users/konrad.malinowski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-advanced-features/05-RESEARCH.md
@.planning/phases/05-advanced-features/05-PATTERNS.md
@.planning/phases/05-advanced-features/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SarifReporter and register it</name>
  <files>src/llm_scanner/reporters/sarif.py, src/llm_scanner/reporters/__init__.py</files>
  <read_first>
    - src/llm_scanner/reporters/json_reporter.py — save() interface pattern: output_dir.mkdir(parents=True, exist_ok=True), path write_text with encoding="utf-8", return path
    - src/llm_scanner/reporters/__init__.py — current _REPORTER_FACTORIES dict, __all__, get_file_reporter() return type annotation
    - src/llm_scanner/models.py — Severity enum values (CRITICAL, HIGH, MEDIUM, LOW, INFO), AttackResult fields (attack_id, owasp_category, name, judge_reasoning, severity, success, recommendation, suppressed, suppression_reason), ScanReport fields (target, findings)
    - .planning/phases/05-advanced-features/05-PATTERNS.md — sarif.py pattern section: _LEVEL_MAP, _build() SARIF structure, synthetic uri ".", logicalLocations, partialFingerprints
    - .planning/phases/05-advanced-features/05-RESEARCH.md — Pattern 2 (SARIF 2.1.0), Code Examples section (SarifReporter complete pattern)
  </read_first>
  <behavior>
    - SarifReporter().save(report, tmp_path) creates tmp_path/report.sarif
    - _build() returns dict with "$schema" == "https://json.schemastore.org/sarif-2.1.0.json"
    - _build() returns dict with "version" == "2.1.0"
    - _build() returns dict with "runs" as a list with exactly one item
    - runs[0]["tool"]["driver"]["rules"] deduplicates by owasp_category (one rule per category)
    - runs[0]["results"] contains only findings where success=True AND not suppressed (getattr(f, "suppressed", False) == False)
    - Each result in runs[0]["results"] has locations[0].physicalLocation.artifactLocation.uri == "."
    - Each result has logicalLocations[0].name == report.target
    - Severity.CRITICAL -> "error", Severity.HIGH -> "error", Severity.MEDIUM -> "warning", Severity.LOW -> "note", Severity.INFO -> "note"
    - get_file_reporter("sarif") returns a SarifReporter instance
    - get_file_reporter("unknown_format") still raises ValueError (existing behavior unchanged)
  </behavior>
  <action>
    Create src/llm_scanner/reporters/sarif.py. Start with `from __future__ import annotations`. Imports: json from stdlib, pathlib.Path, llm_scanner.models (ScanReport, Severity).

    Define _LEVEL_MAP: dict[Severity, str] = {Severity.CRITICAL: "error", Severity.HIGH: "error", Severity.MEDIUM: "warning", Severity.LOW: "note", Severity.INFO: "note"}.

    Define SarifReporter class with two public methods:

    save(self, report: ScanReport, output_dir: Path) -> Path: creates output_dir, writes json.dumps(self._build(report), indent=2) to output_dir / "report.sarif" with encoding="utf-8", returns path.

    _build(self, report: ScanReport) -> dict: (1) Deduplicate rules — iterate report.findings, add rule per unique owasp_category: {"id": category, "shortDescription": {"text": f"OWASP {category}"}, "fullDescription": {"text": f.recommendation or category}, "help": {"text": f.recommendation or "See OWASP Top 10 for LLMs 2025."}}. (2) Build results list — filter to findings where f.success is True AND getattr(f, "suppressed", False) is False; for each: level = _LEVEL_MAP.get(Severity(f.severity), "note"), result dict with ruleId=f.owasp_category, level, message={"text": f"{f.name}: {f.judge_reasoning}"}, locations=[{"physicalLocation": {"artifactLocation": {"uri": "."}, "region": {"startLine": 1, "startColumn": 1}}, "logicalLocations": [{"name": report.target, "kind": "function", "fullyQualifiedName": f"llm-endpoint/{report.target}"}]}], partialFingerprints={"primaryLocationLineHash": f"{f.attack_id}:1"}. (3) Return full SARIF dict with "$schema", "version": "2.1.0", "runs" list containing one run with tool.driver (name="LLM Security Scanner", version="0.1.0", rules) and results.

    Update src/llm_scanner/reporters/__init__.py: add `from llm_scanner.reporters.sarif import SarifReporter` at the top with other imports; add "sarif": SarifReporter to _REPORTER_FACTORIES dict; add "SarifReporter" to __all__; extend get_file_reporter() return type union to include SarifReporter; update the ValueError message to list "md, json, html, txt, sarif".
  </action>
  <verify>
    <automated>cd "/Users/konrad.malinowski/Documents/Konrad/AI-Engineer/AI/LLM Security Scanner" && python -c "from llm_scanner.reporters import get_file_reporter; r = get_file_reporter('sarif'); print(type(r).__name__)" && python -m ruff check src/llm_scanner/reporters/sarif.py src/llm_scanner/reporters/__init__.py</automated>
  </verify>
  <done>
    - python -c "from llm_scanner.reporters import get_file_reporter; get_file_reporter('sarif')" prints "SarifReporter" and exits 0
    - ruff check reports zero errors
    - get_file_reporter("txt") still works (no regression)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_sarif.py</name>
  <files>tests/test_sarif.py</files>
  <read_first>
    - tests/test_reporters.py — fixture pattern (sample_report with two findings: one success=True HIGH, one success=False); import header with `from __future__ import annotations`, datetime UTC, pytest, llm_scanner.models imports; file-write test pattern using tmp_path
    - src/llm_scanner/reporters/sarif.py — SarifReporter._build() output structure to know what keys to assert
    - src/llm_scanner/models.py — Severity enum, AttackResult constructor args
  </read_first>
  <behavior>
    - test_sarif_creates_file: SarifReporter().save(report, tmp_path) returns a path that exists with suffix .sarif
    - test_sarif_schema_fields: parsed SARIF dict has "$schema" == "https://json.schemastore.org/sarif-2.1.0.json" and "version" == "2.1.0"
    - test_sarif_has_one_run: "runs" list has exactly one item
    - test_sarif_only_vulnerable: results list contains only the success=True finding (not the success=False one)
    - test_sarif_location_uri: each result's locations[0]["physicalLocation"]["artifactLocation"]["uri"] == "."
    - test_sarif_level_mapping_critical: finding with severity=CRITICAL produces level "error"
    - test_sarif_level_mapping_medium: finding with severity=MEDIUM produces level "warning"
    - test_sarif_level_mapping_low: finding with severity=LOW produces level "note"
    - test_sarif_suppressed_excluded: finding with success=True but suppressed=True is excluded from results
    - test_sarif_deduplicates_rules: two findings with the same owasp_category produce one rule entry
    - test_sarif_fingerprint: each result has partialFingerprints.primaryLocationLineHash set to "{attack_id}:1"
  </behavior>
  <action>
    Create tests/test_sarif.py. Import header: `from __future__ import annotations`, json, datetime (UTC), pathlib.Path, pytest, llm_scanner.models (AttackResult, ScanReport, Severity), llm_scanner.reporters.sarif (SarifReporter).

    Define a @pytest.fixture sample_report() -> ScanReport with two findings: (1) attack_id="LLM01-001", owasp_category="LLM01", success=True, severity=Severity.HIGH; (2) attack_id="LLM02-001", owasp_category="LLM02", success=False, severity=Severity.LOW. Both need all required fields: name, payload, response, judge_reasoning.

    Define a @pytest.fixture sarif_dict(sample_report) -> dict that calls SarifReporter()._build(sample_report) and returns the dict — reused across multiple tests.

    For test_sarif_suppressed_excluded: create a report with one finding where success=True and suppressed=True; assert len(results) == 0.

    Use json.loads(path.read_text()) to parse the saved SARIF file in file-based tests.

    No @pytest.mark.asyncio needed (all tests are synchronous). asyncio_mode = "auto" is set in pyproject.toml.
  </action>
  <verify>
    <automated>cd "/Users/konrad.malinowski/Documents/Konrad/AI-Engineer/AI/LLM Security Scanner" && python -m pytest tests/test_sarif.py -xq</automated>
  </verify>
  <done>
    - All 11 SARIF tests pass
    - python -m pytest tests/ -q shows no regressions from any previous phase
  </done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by Plan 05-02:

**src/llm_scanner/reporters/sarif.py (new):**
- `_LEVEL_MAP: dict[Severity, str]` (module-level constant)
- `SarifReporter` (class)
- `SarifReporter.save(report: ScanReport, output_dir: Path) -> Path`
- `SarifReporter._build(report: ScanReport) -> dict`

**src/llm_scanner/reporters/__init__.py (modified):**
- `"sarif"` key added to `_REPORTER_FACTORIES`
- `SarifReporter` added to `__all__`
- `get_file_reporter()` return type extended to include `SarifReporter`

**tests/test_sarif.py (new):**
- test_sarif_creates_file, test_sarif_schema_fields, test_sarif_has_one_run, test_sarif_only_vulnerable, test_sarif_location_uri, test_sarif_level_mapping_critical, test_sarif_level_mapping_medium, test_sarif_level_mapping_low, test_sarif_suppressed_excluded, test_sarif_deduplicates_rules, test_sarif_fingerprint

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AttackResult fields → SARIF JSON | Scan findings (attacker-controlled LLM responses) embedded in SARIF output |
| SARIF file → GitHub Security tab | SARIF consumed by GitHub's code scanning engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | SARIF logicalLocations.name (report.target) | accept | Target URL is user-specified CLI arg, not external input; json.dumps() handles all JSON string escaping automatically |
| T-05-05 | Information Disclosure | judge_reasoning in SARIF message.text | accept | SARIF is a local output file authored by the scanner operator; no confidential data beyond what the operator controls |
| T-05-06 | Tampering | Empty artifactLocation.uri | mitigate | Hardcoded uri: "." (non-empty string); GitHub upload-sarif requires non-empty URI — verified against aquasecurity/trivy discussion #7905 |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; no install steps in this plan |
</threat_model>

<verification>
- python -m pytest tests/test_sarif.py -xq: all 11 tests pass
- python -m ruff check src/llm_scanner/reporters/sarif.py src/llm_scanner/reporters/__init__.py: zero errors
- python -c "from llm_scanner.reporters import get_file_reporter; r = get_file_reporter('sarif'); print(type(r).__name__)": prints "SarifReporter"
- python -m pytest tests/ -q: full suite green (no regressions)
</verification>

<success_criteria>
- SarifReporter produces valid SARIF 2.1.0 structure with correct schema URI and version
- Only success=True AND not suppressed findings appear in SARIF results
- artifactLocation.uri is always "." (never empty)
- Severity levels map correctly to SARIF level strings
- get_file_reporter("sarif") returns SarifReporter instance
- Zero ruff errors
</success_criteria>

<output>
Create .planning/phases/05-advanced-features/05-02-SUMMARY.md when done.
</output>
